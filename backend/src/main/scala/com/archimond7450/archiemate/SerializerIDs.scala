package com.archimond7450.archiemate

/** Unique serializer IDs for each persistent actor.
  *
  * Must be non-zero and non-overlapping across all persistent actors.
  */
object SerializerIDs {
  val userTokenActorId: Int = 1001
}
